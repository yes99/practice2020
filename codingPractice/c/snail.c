#include <stdio.h>

int main()
{
int number;
float MAX_NUM;
printf("size of SQUARE: ");
scanf("%d", &number);

if (MAX_NUM % 2 == 1)
{
     
}

printf("%f\n", MAX_NUM); 
printf("repeat time %f", MAX_NUM);

}